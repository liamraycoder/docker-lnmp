upstream hsapp.front.test {
    server php81:8793;
    keepalive 10240;
}

server {
  server_name hsapp.front.test;
  listen 80;

  location / {
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header Host $host;
      proxy_http_version 1.1;
      proxy_set_header Connection "";
      if (!-f $request_filename){
          proxy_pass http://hsapp.front.test;
      }
  }
}
